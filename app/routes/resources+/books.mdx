---
meta:
  title: Books for Christian Entrepreneurs
  description: Books for Christian Entrepreneurs.
---

import { AILevel } from "../../components/ai-level";
import { formatFrontmatterToRemixMeta } from "../../utils/meta";
import { ResourceCard } from "../../components/resources/resource-card";
import { ResourceGrid } from "../../components/resources/resource-grid";
import { inspiration, business } from "../../data/books";
export const meta = formatFrontmatterToRemixMeta(frontmatter);

# Books for Christian Entrepreneurs

## Inspiration

<ResourceGrid>
  {inspiration.map((resource) => (
    <ResourceCard 
      key={resource.title}
      resource={resource}
    />
  ))}
</ResourceGrid>

## Business

<ResourceGrid>
  {business.map((resource) => (
    <ResourceCard 
      key={resource.title}
      resource={resource}
    />
  ))}
</ResourceGrid>
